trigger:
  branches:
    include:
    - master
  paths:
    include:
    - environments/qa
    - Charts
    - modules

extends:
  template: pipeline-templates/pipeline-shared.yaml
  parameters:
    environment: qa
    variablegroup: 'cd-pipeline-variablegroup'
