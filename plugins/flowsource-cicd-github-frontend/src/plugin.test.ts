import { flowsourceCicdCustomFrontendPlugin } from './plugin';

describe('flowsource-cicd-custom-frontend', () => {
  it('should export plugin', () => {
    expect(flowsourceCicdCustomFrontendPlugin).toBeDefined();
  });
});
