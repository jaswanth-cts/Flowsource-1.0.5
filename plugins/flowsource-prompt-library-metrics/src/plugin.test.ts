import { flowsourcePromptLibraryMetricsPlugin } from './plugin';

describe('flowsource-prompt-library-metrics', () => {
  it('should export plugin', () => {
    expect(flowsourcePromptLibraryMetricsPlugin).toBeDefined();
  });
});
