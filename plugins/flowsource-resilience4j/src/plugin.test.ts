import { flowsourceResilience4JPlugin } from './plugin';

describe('flowsource-resilience4j', () => {
  it('should export plugin', () => {
    expect(flowsourceResilience4JPlugin).toBeDefined();
  });
});
