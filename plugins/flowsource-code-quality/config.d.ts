export interface Config {
    sonarqube: {
      /**
       * Frontend root URL
       * @visibility frontend
       */
      projectUrl: string; // defined in core, but repeated here without doc
    };
  }

