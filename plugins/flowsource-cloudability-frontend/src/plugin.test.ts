import { flowsourceCloudabilityFrontendPlugin } from './plugin';

describe('flowsource-cloudability-frontend', () => {
  it('should export plugin', () => {
    expect(flowsourceCloudabilityFrontendPlugin).toBeDefined();
  });
});
