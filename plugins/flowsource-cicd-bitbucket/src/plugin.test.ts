import { flowsourceCicdBitbucketPlugin } from './plugin';

describe('flowsource-cicd-bitbucket', () => {
  it('should export plugin', () => {
    expect(flowsourceCicdBitbucketPlugin).toBeDefined();
  });
});
