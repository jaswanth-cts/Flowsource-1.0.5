import { makeStyles } from '@material-ui/core';

const cssClasses = makeStyles((theme) => ({

  accordingDetailsStyle: {
    display: 'block',
    width: '100% !important',
  },


  accordingDetailsPrismaStyle: {
    display: 'block',
    width: '100% !important',
  },
 
}));

export default cssClasses;