import { flowsourceWellArchitectedPlugin } from './plugin';

describe('flowsource-well-architected', () => {
  it('should export plugin', () => {
    expect(flowsourceWellArchitectedPlugin).toBeDefined();
  });
});
