import { flowsourceDevopsGuruPlugin } from './plugin';

describe('flowsource-devops-guru', () => {
  it('should export plugin', () => {
    expect(flowsourceDevopsGuruPlugin).toBeDefined();
  });
});
