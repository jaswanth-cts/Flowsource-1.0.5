import { flowsourceDoraMetricsPlugin } from './plugin';

describe('flowsource-dora-metrics', () => {
  it('should export plugin', () => {
    expect(flowsourceDoraMetricsPlugin).toBeDefined();
  });
});
